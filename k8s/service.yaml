apiVersion: v1
kind: Service
metadata:
  name: nemotron-frontend
  namespace: nemotron
spec:
  selector:
    app: nemotron-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  type: ClusterIP